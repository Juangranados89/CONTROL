# Example environment variables for the server
# On Render set these in the service Environment

# Postgres connection string example:
# postgres://<username>:<password>@<host>:<port>/<database>
DATABASE_URL="postgresql://postgres:password@localhost:5432/fleet_db?schema=public"

# Secret for JWT tokens
JWT_SECRET="replace-with-a-secure-secret"

# Optional: node version hint
NODE_ENV=production
